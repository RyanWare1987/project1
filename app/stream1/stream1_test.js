'use strict';

describe('myApp.stream1 module', function() {

  beforeEach(module('myApp.stream1'));

  describe('stream1 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var stream1Ctrl = $controller('stream1Ctrl');
      expect(stream1Ctrl).toBeDefined();
    }));

  });
});